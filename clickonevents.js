/*let addedEvents = document.getElementsByClassName("calendar-event");

for (let i = 0; i < addedEvents.length; i++) {
    addedEvents[i].addEventListener("click", showDetails);
}

function showDetails() {
    var eventModal = document.createElement("div");
    var body = document.getElementsByTagName("body")[0];
    eventModal.classList.add("modal");

    body.appendChild(eventModal);

    eventModal.innerHTML = `<div class="event-modal"><label>Title: Haroon</label>
    <span>Date: 12/12/12</span></div>
    `;
}*/

/*prueba
function getIds() {
    let n = document.querySelectorAll(".calendar-event");

    for (let i = 0; i < n.length; i++) {
        n = parseInt(n[i].getAttribute("id"));

        console.log(n);
    }
}

getIds(); */